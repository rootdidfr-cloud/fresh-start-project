import React from 'react';
import { LayoutGrid, CircleDot, Coins, Trophy, Gift, Users, ShieldCheck, HelpCircle, ArrowUpRight, ChevronRight, X, Sparkles } from 'lucide-react';
import { Logo } from './ui';
export default function Sidebar({page,setPage,openModal,mobileOpen,setMobileOpen,flipCount}) {
  function nav(p) {setPage(p); setMobileOpen(false); window.scrollTo({top:0,behavior:'smooth'});}
  const links = [{name:'Overview',icon:LayoutGrid,id:'overview'},{name:'Jackpot',icon:CircleDot,id:'jackpot',badge:'LIVE'},{name:'Coinflip',icon:Coins,id:'coinflip',count:String(flipCount)}];
  return <><div className={`mobile-scrim ${mobileOpen?'visible':''}`} onClick={()=>setMobileOpen(false)}/><aside className={`sidebar ${mobileOpen?'mobile-open':''}`}><button className="logo-button" onClick={()=>nav('overview')} aria-label="Bpot home"><Logo/></button><button className="icon-button mobile-close" onClick={()=>setMobileOpen(false)} aria-label="Close navigation"><X size={20}/></button><div className="sidebar-inner"><div className="nav-label">THE PLAYGROUND</div><nav>{links.map(({name,icon:Icon,id,badge,count})=><button key={id} className={`nav-item ${page===id?'active':''}`} onClick={()=>nav(id)}><Icon size={19}/><span>{name}</span>{badge&&<b className="live-mini">{badge}</b>}{count&&<b className="nav-count">{count}</b>}</button>)}</nav><div className="nav-divider"/><nav><button className={`nav-item ${page==='leaderboard'?'active':''}`} onClick={()=>nav('leaderboard')}><Trophy size={19}/><span>Leaderboard</span></button><button className="nav-item" onClick={()=>openModal('rewards')}><Gift size={19}/><span>Rewards</span><span className="little-dot"/></button><button className="nav-item" onClick={()=>openModal('affiliates')}><Users size={19}/><span>Affiliates</span></button></nav><button className="token-teaser" onClick={()=>openModal('token')}><span className="token-teaser-top"><Sparkles size={15}/><span>SOMETHING BIG</span></span><strong>The next chapter.<br/>Powered by you.</strong><span className="token-teaser-bottom">The Bpot token <ArrowUpRight size={15}/></span><div className="teaser-orbit"/></button><div className="sidebar-bottom"><button className="nav-item quiet" onClick={()=>openModal('fairness')}><ShieldCheck size={17}/><span>Fair play</span><ArrowUpRight size={13}/></button><button className="nav-item quiet" onClick={()=>openModal('support')}><HelpCircle size={17}/><span>Help & support</span><ArrowUpRight size={13}/></button><div className="sidebar-footer"><span className="network-dot"/>Demo environment<span className="network-status">Preview online</span></div><div className="social-row"><a href="https://x.com/bpotfun" target="_blank" rel="noopener noreferrer" aria-label="Bpot on X">𝕏</a><button onClick={()=>openModal('community')} aria-label="Bpot community"><Users size={17}/></button><span>© 2026 Bpot</span></div></div></div></aside></>;
}
